# Auto-provision Prometheus as a Grafana data source
apiVersion: 1

datasources:
  - name: Prometheus
    type: prometheus
    access: proxy
    url: http://lms-prometheus:9090
    isDefault: true
    editable: true
